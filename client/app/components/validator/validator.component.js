import template from './validator.html';
import controller from './validator.controller';
import './validator.styl';

let validatorComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default validatorComponent;
